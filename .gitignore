.homeboy-build/
build/*.zip

# NEVER commit a backup-profile export — it carries LIVE credentials.
# Only the share profile (secrets stripped) belongs in this repo.
*backup*/
*-backup/
bundles/**/backup/
